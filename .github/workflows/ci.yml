name: CI Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-unix:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y g++ make libncursesw5-dev zip unzip

      - name: Install dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          # macOS provides clang++ with C++17 support, ncurses, zip, and unzip
          # out of the box. Install Python packages for doc generation.
          pip3 install --break-system-packages markdown weasyprint || pip3 install markdown weasyprint || true

      - name: Build
        run: make

      #- name: Build and run tests
      #  run: make runtest

      - name: Extract version
        id: version
        run: |
          ver=$(grep 'PROGRAM_VERSION' src/program_info.h | sed -E 's/.*"(.*)".*/\1/' | tr -d '.')
          echo "ver=$ver" >> "$GITHUB_OUTPUT"

      - name: Prepare release directory
        run: |
          make nixReleaseArc RELEASE_ARC_FLAGS="--no-zip"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: SlyMail-${{ steps.version.outputs.ver }}-${{ runner.os }}
          path: |
            FILE_ID.DIZ
            SlyMail_*/
          retention-days: 90

  build-windows:
    name: Build (Windows)
    runs-on: windows-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build Release (x64)
        run: msbuild vs\SlyMail.sln /p:Configuration=Release /p:Platform=x64 /m

      - name: Extract version
        id: version
        shell: bash
        run: |
          ver=$(grep 'PROGRAM_VERSION' src/program_info.h | sed -E 's/.*"(.*)".*/\1/' | tr -d '.')
          echo "ver=$ver" >> "$GITHUB_OUTPUT"

      - name: Prepare Windows release directory
        shell: cmd
        run: makeWindowsReleaseArchive.bat --no-zip

      - name: Upload Windows release artifact
        uses: actions/upload-artifact@v4
        with:
          name: SlyMail-${{ steps.version.outputs.ver }}-Windows
          path: |
            FILE_ID.DIZ
            SlyMail_Windows/
          retention-days: 90
